

rnorm()

c()
seq()
#outra forma de usar o seq é indicar de qual número até qual número , e também indicar o tamanho do vector a ser criado

seq(from=10, to=20, length.out = 100)


#outra forma é usar o along.with 

#ex :


x<- c("a","b","c")


seq(from=10,to=20, along.with = x)



rep()

#podemos usar o times ou o each , mostrando quantos cada um deve ser repetido
rep(5:6, times = 10)

rep(x, each = 2)


print()

is.numeric()
is.integer()
is.double()
is.character()


typeof()

sqrt()
paste()



#? 

#para usar o help e descobrir o que a função faz etc é so add um ? antes da função


?typeof

